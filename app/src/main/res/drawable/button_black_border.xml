<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/black"/>  <!-- Button background color -->
    <stroke android:width="2dp" android:color="@android:color/black"/> <!-- Border width and color -->
    <corners android:radius="25dp"/>  <!-- Rounded corners -->
    <padding android:left="10dp" android:top="5dp" android:right="10dp" android:bottom="5dp"/> <!-- Padding -->
</shape>
